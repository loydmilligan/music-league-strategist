# Music League Strategist Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# OpenRouter API (Required for AI features)
# ===========================================
# Get your API key at: https://openrouter.ai/keys
VITE_OPENROUTER_API_KEY=

# Default AI model to use
VITE_DEFAULT_MODEL=anthropic/claude-sonnet-4

# ===========================================
# Spotify OAuth 2.0 (Optional - for playlists)
# ===========================================
# Set up at: https://developer.spotify.com/dashboard
# Requires: Web API scope, refresh token from OAuth flow
VITE_SPOTIFY_CLIENT_ID=
VITE_SPOTIFY_CLIENT_SECRET=
VITE_SPOTIFY_REFRESH_TOKEN=

# ===========================================
# YouTube Data API OAuth 2.0 (Optional - for playlists)
# ===========================================
# Set up at: https://console.cloud.google.com/apis/credentials
# Requires: YouTube Data API v3 enabled, OAuth 2.0 credentials
VITE_YOUTUBE_CLIENT_ID=
VITE_YOUTUBE_CLIENT_SECRET=
VITE_YOUTUBE_REFRESH_TOKEN=

# ===========================================
# Authentication (Required for user auth)
# ===========================================
# JWT secret for signing tokens (use a long random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT token expiration times
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d

# ===========================================
# Gmail SMTP (Required for email verification)
# ===========================================
# Enable 2FA on your Google account, then generate an App Password:
# https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password

# Email sender address (can be same as SMTP_USER)
SMTP_FROM=noreply@yourdomain.com

# App URL for email links (no trailing slash)
APP_URL=https://mlstrat.mattmariani.com
